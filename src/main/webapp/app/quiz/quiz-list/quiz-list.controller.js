(function() {
    'use strict';

    angular
        .module('springDemoApp')
        .controller('QuizListController', QuizListController);

    QuizListController.$inject = ['$filter'];

    function QuizListController (filter) {
        var vm = this;

        
    }
})();
